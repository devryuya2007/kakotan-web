# 要件

## PDF 取得とテキスト化の実行手順（簡易）

1. 依存のインストール

```bash
python3 -m pip install --user -r requirements.txt
```

2. ダウンロード＋変換（例）

```bash
bash scripts/fetch_kyotsu.sh ~/kyotsu_english
```

3. 検証

- `~/kyotsu_english` に PDF が保存されること
- `texts/` に各年度の .txt が生成されること
